import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
    tabs: {
        width: '100%',
    },
    title: {
        textTransform: 'uppercase',
        whiteSpace: 'nowrap',
        padding: '5px 40px',
    },
    button: {
        marginTop: '4px',
        marginLeft: '10px',
    }
});